#include <stdlib.h> 
 #include <time.h> 
#include <stdio.h>
/**
 * main - entry point 
 *
 * Description: print value of n grearer than 0 
 *
 * Return: 0 ( sucsses
 

  int main(void) 
 { 
         int n;,digit;
  
	srand(time(0)); 
	n = rand() - RAND_MAX /2; 
        digit = % 10;

	if (digit > 5)
	        printf( digist)
        elso if (digit ==0)
	         printf(last dugit of %i greater than r;

	 return (0); 
 }

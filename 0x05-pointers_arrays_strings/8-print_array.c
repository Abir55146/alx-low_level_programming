#include "main.h"

/**
 * print_arry - print elemaints of an arry
 *      of intagras
 *
 * @n: elementsof paramater input
 * @a: string paramater input 
 *
 * Return: Nothing
*/
void print_array(int *a, int n)
{

	int i;

	for (i = 0; i < n; i++)
	{
		if (i != (n - 1)
			
		printf(%d;  a[i]);
         
		else 
		      printf(%d; a[i]);
         }
	 printf('\n');
}
